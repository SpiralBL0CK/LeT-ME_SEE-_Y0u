#!/usr/bin/env python
import threading
from Sql import Sql_INject
from Brute import Forcez
from Spam import Spam
from Scan import Scanz
from Crawl import crawl

def main():
    #pydoc.doc(find_open_port)
    #Scanz.find_open_port(host)
    #Scanz.connect(host,port,message)
    #crawl.grabme()
    #links = crawl.grabLinks("/tutorial")
    #title = crawl.getName("http://www.pythonscraping.com/pages/page1.html")
    #if title == None:
     #   print("Title could not be found")
    #else:
     #   print(title)
    #links = crawl.getALink('/tutorial')
    #crawl.grabLinks("/tutorial")
    #crawl.crawlDoc("")
    #crawl.followExternalOnly("https://www.google.com")
    #crawl.getAllExternalLinks("https://www.twitter.com")
    #Spam.smap()
    #hime()
    #Spam.fmap()
    #Spam.spam_sms()
    #Sql_INject.sql_automate("http://www.romanianwriters.ro/s.php")
    #for i in range(100):
        #t = threading.Thread(target=Spam.attack())
        #t.start()

if __name__ == '__main__':
    main()
